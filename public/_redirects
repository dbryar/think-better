# Routes are materialized as physical index.html files during the Vite build.
# Keep this file intentionally empty of SPA fallbacks so route-matching .md
# files such as /cards/who-benefits.md remain directly fetchable.
